# Multiplayer fix (Selenium / Chrome) — dependencies are listed in the main requirements.
# Install everything with: pip install -r requirements.txt
# If grpcio-tools fails, use: pip install -r requirements-consumer.txt
-r requirements.txt
